SET(CURL_LIBRARY "-lcurl") 
FIND_PACKAGE(CURL REQUIRED) 
ADD_LIBRARY(miner
  source/miner.cxx
)
TARGET_INCLUDE_DIRECTORIES(miner PUBLIC include)
TARGET_INCLUDE_DIRECTORIES(miner PRIVATE ${CURL_INCLUDE_DIR})
TARGET_LINK_LIBRARIES(miner utils ${CURL_LIBRARIES})